# Задача 1: Создание и вызов функций
# Создайте функцию с именем `say_hello`. Эта функция не принимает аргументов.
# При вызове она должна выводить в консоль строку "Привет, мир!".
# После определения функции вызовите её один раз.
from curses import wrapper
from datetime import datetime
from operator import ifloordiv
from tabnanny import check
from tkinter.font import names


def say_hello():
    print("Привет, мир!")

# Задача 2: Аргументы функций
# Создайте функцию с именем `multiply`.
# Функция должна принимать два аргумента (числа) с именами `x` и `y`.
# Внутри функции вычислите произведение `x` и `y` и выведите результат в консоль.
# Вызовите функцию, передав ей в качестве аргументов числа 5 и 7.
def multiply(x, y):
    print(x * y)
multiply(5,7)

# Задача 3: Возвращение данных из функций
# Создайте функцию с именем `get_max`.
# Функция должна принимать два числовых аргумента `a` и `b`.
# Функция должна сравнивать эти числа и ВОЗВРАЩАТЬ (return) большее из них.
# Не выводите результат в консоль внутри функции.
# После определения функции вызовите её с аргументами 12 и 9.
# Результат вызова функции присвойте переменной `result` и затем выведите значение этой переменной на экран.
def get_max(a, b):
    return a if a > b else b
result = get_max(12, 9)
print(result)

# Задача 4: Аргументы с дефолтными значениями
# Создайте функцию с именем `greet_user`.
# Функция должна принимать один обязательный аргумент `name` и один необязательный аргумент `greeting` со значением по умолчанию "Добро пожаловать".
# Функция должна ВОЗВРАЩАТЬ (return) строку в формате: `f"{greeting}, {name}!"`.
# Вызовите функцию дважды и выведите результаты каждого вызова в консоль:
# 1) С одним аргументом: `"Анна"`.
# 2) С двумя аргументами: `"Петр"` и `"Приветствую"`.
def greet_user(name, greeting="Добро пожаловать"):
    return f"{greeting}, {name}!"
print(greet_user("Анна"))
print(greet_user("Петр", "Приветствую"))

# Задача 5: Декораторы
# Создайте простой декоратор с именем `my_decorator`.
# Декоратор должен делать следующее:
# 1) Перед вызовом декорируемой функции печатать строку: "Функция вызвана".
# 2) Вызывать саму декорируемую функцию.
# 3) После вызова декорируемой функции печатать строку: "Вызов функции завершен".
# Примените этот декоратор к функции `example`, которая просто печатает "Привет!".
# Вызовите декорированную функцию `example`.
def my_decorator(func):
    def wrapper():
        print("Функция вызвана")
        func()
        print("Вызов функции завершен")
    return wrapper
@my_decorator
def example():
        print("Привет!")
example()

# Задача 6: Создание и вызов функций
# Создайте функцию с именем `print_square`.
# Функция должна принимать один аргумент `side` (длина стороны квадрата).
# Функция должна вычислять площадь квадрата по формуле `side * side` и ВЫВОДИТЬ (print) результат в консоль в формате: "Площадь квадрата: <результат>".
# Вызовите функцию с аргументом 5.
def print_square(side):
    result = side * side
    print(f"Площадь квадрата: {result}")
print_square(5)

# Задача 7: Аргументы функций
# Создайте функцию с именем `connect_strings`.
# Функция должна принимать два строковых аргумента `str1` и `str2` и один необязательный числовой аргумент `repeat` со значением по умолчанию 1.
# Функция должна ВОЗВРАЩАТЬ (return) одну строку, сформированную по правилу:
#   `str1` + `str2`, и эта склеенная строка повторяется `repeat` раз.
# Например, при `str1="A"`, `str2="B"`, `repeat=3` результат должен быть "ABABAB".
# Вызовите функцию один раз с аргументами `"Hello"`, `"World"` и `repeat=2`, присвойте результат переменной и выведите её.
def connect_strings(str1, str2, repeat=2):
    result = (str1 + str2) * repeat
    return result
my_result = connect_strings("Hello", "World",2)
print(my_result)

# Задача 8: Возвращение данных из функций
# Создайте функцию с именем `is_even`.
# Функция должна принимать один целочисленный аргумент `number`.
# Функция должна ВОЗВРАЩАТЬ (return) булево значение:
#   `True`, если число чётное,
#   `False`, если число нечётное.
# В самой функции НЕЛЬЗЯ использовать `print`.
# После определения функции вызовите её с аргументом 10, присвойте результат переменной `check` и выведите значение этой переменной.
def is_even(number):
    return number % 2 == 0
check = is_even(10)
print(check)

# Задача 9: Декораторы
# Создайте декоратор с именем `repeat_three_times`.
# Декоратор должен задекорированную функцию вызывать ровно ТРИ раза подряд.
# Сам декоратор не должен ничего печатать.
# Примените этот декоратор к функции `say_hi`, которая выводит в консоль строку "Hi!".
# После применения декоратора вызов `say_hi()` должен привести к троекратному выводу "Hi!".

def repeat_three_times(func):
    def wrapper():
        for i in range(3):
            func()
    return wrapper

@repeat_three_times
def say_hi():
    print("Hi!")
say_hi()

# Задача 10: Аргументы с дефолтными значениями
# Создайте функцию с именем `create_profile`.
# Функция должна принимать три аргумента со значениями по умолчанию:
#   `name` (строка, по умолчанию "Гость")
#   `age` (число, по умолчанию 18)
#   `city` (строка, по умолчанию "Москва")
# Функция должна ВОЗВРАЩАТЬ (return) словарь следующего вида:
#   {"имя": name, "возраст": age, "город": city}
# Вызовите функцию три раза и выведите результаты:
# 1) Без аргументов (все значения по умолчанию).
# 2) Только с аргументом `name="Анна"`.
# 3) С аргументами `name="Петр"`, `city="Казань"` (возраст по умолчанию).
def create_profile(name="Гость",age=18, city="Москва"):
    return{
        "имя": name,
        "возраст": age,
        "город": city
    }
print(create_profile())
print(create_profile(name="Анна"))
print(create_profile(name="Петр", city="Казань"))

# Задача 11: Создание и вызов функций
# Создайте функцию с именем `describe_pet`.
# Функция должна принимать два аргумента:
#   `animal_type` (строка, например, "собака")
#   `pet_name` (строка)
# Функция должна ВЫВОДИТЬ (print) строку в формате:
#   "У меня есть <animal_type>, её зовут <pet_name>."
# Вызовите функцию два раза с разными наборами аргументов.
def describe_pet(animal_type, pet_name):
    print(f"У меня есть {animal_type},её зовут {pet_name}")
describe_pet("Собака", "Шарик")
describe_pet("Кот","Мурзик")

# Задача 12: Аргументы функций
# Создайте функцию с именем `calculate`.
# Функция должна принимать три числовых аргумента: `a`, `b` и `operation`.
# Аргумент `operation` — строка, которая может быть "+" или "-".
# Функция должна ВОЗВРАЩАТЬ (return) результат выполнения указанной операции над числами a и b.
# Если операция не "+" и не "-", функция должна возвращать строку "Неизвестная операция".
# Вызовите функцию три раза и выведите результаты:
# 1) a=5, b=3, operation="+"
# 2) a=10, b=7, operation="-"
# 3) a=2, b=2, operation="*"

def calculate(a, b, operation):
    if operation == "+":
        return a + b
    elif operation == "-":
        return a - b
    else:
        return "Неизвестная операция"
print(calculate(5, 3, "+"))
print(calculate(10, 7, "-"))
print(calculate(2, 2, "*"))

# Задача 13: Возвращение данных из функций
# Создайте функцию с именем `sum_two`.
# Функция должна принимать два числовых аргумента `x` и `y`.
# Функция должна ВОЗВРАЩАТЬ (return) их сумму.
# Не выводите результат внутри функции.
# После определения функции вызовите её с аргументами 15 и 25.
# Результат вызова присвойте переменной `total` и выведите значение `total` на экран.
def sum_two(x, y):
    return x + y
total = sum_two(15, 25)
print(total)

# Задача 14: Декораторы
# Создайте декоратор с именем `add_exclamation`.
# Декоратор должен модифицировать результат декорируемой функции, добавляя к нему восклицательный знак.
# Предполагается, что декорируемая функция возвращает строку.
# Внутри декоратора:
#   1) Получите результат вызова оригинальной функции.
#   2) Добавьте к этому результату "!" в конце.
#   3) Верните новую строку.
# Примените декоратор к функции `say_word`, которая возвращает строку "Привет".
# Вызовите декорированную функцию и выведите её результат.
def add_exclamation(func):
    def wrapper():
        original_result = func()
        new_result = original_result + "!"
        return new_result
    return wrapper
@add_exclamation
def say_word():
    return ("Привет")
print(say_word())

# Задача 15: Аргументы с дефолтными значениями
# Создайте функцию с именем `show_message`.
# Функция должна принимать один обязательный аргумент `text` (строка) и один необязательный аргумент `count` (целое число) со значением по умолчанию 1.
# Функция должна ВЫВОДИТЬ (print) переданный текст `text` столько раз, сколько указано в `count` (каждый раз с новой строки).
# Вызовите функцию два раза:
# 1) С одним аргументом: `text="Один раз"`.
# 2) С двумя аргументами: `text="Три раза"`, `count=3`.
def show_message(text, count=1):
    for i in range(count):
        print(text)
show_message("Один раз")
show_message("Три раза", 3)

# Задача 16: Создание и вызов функций
# Создайте функцию с именем `print_rectangle`.
# Функция должна принимать два числовых аргумента: `width` (ширина) и `height` (высота).
# Функция должна ВЫВОДИТЬ (print) сообщение в формате:
#   "Прямоугольник размером <width>x<height>"
# Вызовите функцию один раз с аргументами 10 и 5.
def print_rectangle(width, height):
    print(f"Прямоугольник размером {width}x{height}")
print_rectangle(10, 5)

# Задача 17: Аргументы функций
# Создайте функцию с именем `is_positive`.
# Функция должна принимать один числовой аргумент `num`.
# Функция должна ВОЗВРАЩАТЬ (return) булево значение:
#   `True`, если число больше нуля,
#   `False` — в противном случае (если число меньше или равно нулю).
# В самой функции НЕЛЬЗЯ использовать `print`.
# После определения функции вызовите её с аргументом 7, присвойте результат переменной `check` и выведите значение этой переменной.
def is_positive(num):
    return num > 0
check = is_positive(7)
print(check)

# Задача 18: Возвращение данных из функций
# Создайте функцию с именем `make_upper`.
# Функция должна принимать одну строку `text`.
# Функция должна ВОЗВРАЩАТЬ (return) эту же строку, преобразованную к ВЕРХНЕМУ регистру.
# Не выводите ничего внутри функции.
# Вызовите функцию с аргументом "привет, мир!", присвойте результат переменной `shout` и выведите эту переменную.
def make_upper(text):
    return text.upper()
shout = make_upper("привет, мир!")
print(shout)

# Задача 19: Декораторы
# Создайте декоратор с именем `double_result`.
# Декоратор должен делать следующее:
#   1) Вызвать оригинальную функцию.
#   2) Полученный результат УМНОЖИТЬ НА 2.
#   3) Вернуть это новое значение.
# Предполагается, что декорируемая функция возвращает число.
# Примените декоратор к функции `get_number`, которая возвращает число 10.
# Вызовите декорированную функцию и выведите её результат.
def double_result(func):
    def wrapper():
        original_result = func() * 2
        return original_result
    return wrapper
@double_result
def get_number():
    return 10
print(get_number())

# Задача 20: Аргументы с дефолтными значениями
# Создайте функцию с именем `power`.
# Функция должна принимать два числовых аргумента: `base` (основание) и `exponent` (степень) со значением по умолчанию 2.
# Функция должна ВОЗВРАЩАТЬ (return) результат возведения `base` в степень `exponent`.
# Вызовите функцию два раза и выведите результаты:
# 1) С двумя аргументами: `base=3`, `exponent=4`.
# 2) С одним аргументом: `base=5` (степень должна взять значение по умолчанию).
def power(base, exponent=2):
    return base ** exponent
print(power(3, 4))
print(power(5))

# Задача 21: Создание и вызов функций
# Создайте функцию с именем `ask_name`.
# Функция не должна принимать аргументов.
# Внутри функции с помощью встроенной функции `input()` спросите у пользователя "Как вас зовут?".
# Функция должна ВОЗВРАЩАТЬ (return) введённое пользователем имя (строку).
# После определения функции вызовите её, результат присвойте переменной `user_name`.
# Выведите приветствие в формате: "Привет, <user_name>!"
def ask_name():
    name = input("Как вас зовут?")
    return name
user_name = ask_name()
print(f"Привет, {user_name}!")

# Задача 22: Аргументы функций
# Создайте функцию с именем `compare`.
# Функция должна принимать два числовых аргумента `x` и `y`.
# Функция должна ВОЗВРАЩАТЬ (return) строку:
#   - "Больше", если x > y
#   - "Меньше", если x < y
#   - "Равно", если x == y
# В самой функции НЕЛЬЗЯ использовать `print`.
# Вызовите функцию с аргументами 8 и 5, присвойте результат переменной `res` и выведите её.
def compare(x, y):
    if x > y:
       return "Больше"
    elif x < y:
       return "Меньше"
    elif x == y:
       return "Равно"
res = compare(8, 5)
print(res)

# Задача 23: Возвращение данных из функций
# Создайте функцию с именем `repeat_word`.
# Функция должна принимать два аргумента:
#   `word` (строка) и `times` (целое число, по умолчанию 1).
# Функция должна ВОЗВРАЩАТЬ (return) строку,
#   где `word` повторяется `times` раз, разделённые пробелом.
# Например: repeat_word("Амиго", 3) -> "Амиго Амиго Амиго"
# Вызовите функцию дважды и выведите результаты:
# 1) repeat_word("Окей")
# 2) repeat_word("Стоп", 3)
def repeat_word(word, times=1):
    result = (word + " ") * times
    return result.strip()
print(repeat_word("Окей", 2))
print(repeat_word("Стоп", 3))

# Задача 24: Декораторы
# Создайте декоратор с именем `log_call`.
# Декоратор должен перед вызовом декорируемой функции печатать строку:
#   "Вызывается функция <имя функции>"
# Затем вызывать саму функцию.
# Примените декоратор к функции `test`, которая выводит "Тест выполнен".
# Вызовите декорированную функцию.
def log_call(func):
    def wrapper():
        print(f"Вызывается функция {func.__name__}")
        result = func()
        return result
    return wrapper
@log_call
def test():
    print("Тест выполнен")
test()

# Задача 25: Аргументы с дефолтными значениями (простая)
# Создайте функцию с именем `format_date_simple`.
# Функция должна принимать три аргумента с значениями по умолчанию:
#   `day` (число, по умолчанию 1)
#   `month` (число, по умолчанию 1)
#   `year` (число, по умолчанию 2000)
# Функция должна ВОЗВРАЩАТЬ (return) строку в формате "<day>.<month>.<year>",
#   где числа подставляются как есть (без добавления нулей).
# Вызовите функцию два раза и выведите результаты:
# 1) Без аргументов (все значения по умолчанию).
# 2) С аргументами day=14, month=7, year=2023.
def format_date_simple(day=1, month=1, year=2000):
    return str(day) + "." + str(month) + "." + str (year)
print(format_date_simple())
print(format_date_simple(14, 7, 2023))

# Задача 26: Создание и вызов функций
# Создайте функцию с именем `print_line`.
# Функция должна принимать один строковый аргумент `symbol`.
# Функция должна ВЫВОДИТЬ (print) горизонтальную линию из 10 символов `symbol`.
# Вызовите функцию с аргументом "*".
def print_line(symbol):
    print(symbol * 10)
print_line("*")

# Задача 27: Аргументы функций
# Создайте функцию с именем `check_password`.
# Функция должна принимать один строковый аргумент `password`.
# Функция должна ВОЗВРАЩАТЬ (return) булево значение:
#   `True`, если длина пароля больше или равна 8,
#   `False` — в противном случае.
# В самой функции НЕЛЬЗЯ использовать `print`.
# Вызовите функцию с аргументом "qwerty", присвойте результат переменной `is_strong` и выведите эту переменную.
def check_password(password):
    return len(password) >= 8
is_strong = check_password("qwerty")
print(is_strong)

# Задача 28: Возвращение данных из функций
# Создайте функцию с именем `first_and_last`.
# Функция должна принимать один строковый аргумент `text`.
# Функция должна ВОЗВРАЩАТЬ (return) строку,
#   состоящую из первого и последнего символа переданной строки,
#   соединённых вместе.
# Например: first_and_last("Python по блокам.") -> "Pn"
# Если строка пустая или состоит из одного символа, верните эту строку как есть.
# Вызовите функцию с аргументом "Автоматизация", присвойте результат переменной и выведите её.
def first_and_last(text):
    if len(text) <=1 :
        return text
    else:
        return text[0]+text[-1]
result = first_and_last("Автоматизация")
print(result)

# Задача 29: Декораторы
# Создайте декоратор с именем `say_bye_after`.
# Декоратор должен делать следующее:
#   1) Вызвать оригинальную функцию.
#   2) После её выполнения вывести на экран слово "Пока!".
# Примените декоратор к функции `say_hello`, которая выводит "Привет!".
# Вызовите декорированную функцию.
def say_bye_after(func):
    def wrapper():
        result = func()
        print("Пока!")
    return wrapper
@say_bye_after
def say_hello():
    print("Привет!")
say_hello()

# Задача 30.: Аргументы с дефолтными значениями
# Создайте функцию с именем `order_coffee`.
# Функция должна принимать два аргумента:
#   `coffee_type` (строка, по умолчанию "эспрессо")
#   `size` (строка, по умолчанию "средний")
# Функция должна ВОЗВРАЩАТЬ (return) строку в формате:
#   "Ваш заказ: <coffee_type>, размер <size>"
# Вызовите функцию три раза и выведите результаты:
# 1) Без аргументов (все значения по умолчанию).
# 2) С одним аргументом: `coffee_type="латте"`.
# 3) С двумя аргументами: `coffee_type="капучино"`, `size="большой"`.
def order_coffee(coffee_type="эспрессо", size="средний"):
    return f"Ваш заказ: {coffee_type}, размер {size}"
print(order_coffee())
print(order_coffee("латте"))
print(order_coffee(coffee_type="капучино", size="большой"))

# Задача 31: Создание и вызов функций
# Создайте функцию с именем `show_info`.
# Функция должна принимать два аргумента:
#   `name` (строка) и `age` (число).
# Функция должна ВЫВОДИТЬ (print) сообщение в формате:
#   "Имя: <name>, Возраст: <age>"
# Вызовите функцию с аргументами "Анна" и 25.
def show_info(name, age):
    print(f"Имя: {name}, Возраст: {age}")
show_info("Анна", 25)

# Задача 32: Аргументы функций
# Создайте функцию с именем `concat_strings`.
# Функция должна принимать два строковых аргумента `a` и `b`.
# Функция должна ВОЗВРАЩАТЬ (return) результат их конкатенации (склеивания).
# Вызовите функцию с аргументами "Hello, " и "World!".
# Результат присвойте переменной `message` и выведите её.
def concat_strings(a, b):
    return a + b
message = concat_strings("Hello, ", "World!")
print(message)

# Задача 33: Возвращение данных из функций
# Создайте функцию с именем `square`.
# Функция должна принимать один числовой аргумент `n`.
# Функция должна ВОЗВРАЩАТЬ (return) квадрат этого числа (n * n).
# Вызовите функцию с аргументом 9, результат присвойте переменной `result` и выведите её.
def square(num):
    return num * num
result = square(9)
print(result)

# Задача 34: Декораторы
# Создайте декоратор с именем `add_stars`.
# Декоратор должен делать следующее:
#   1) До вызова функции вывести строку: "***"
#   2) Вызвать оригинальную функцию.
#   3) После вызова функции вывести строку: "***"
# Примените декоратор к функции `print_message`, которая выводит "Привет".
# Вызовите декорированную функцию.
def add_stars(func):
    def wrapper():
        print("***")
        original_result = func()
        print("***")
        return original_result
    return wrapper
@add_stars
def print_message():
   print("Привет")
print_message()

# Задача 35: Аргументы с дефолтными значениями
# Создайте функцию с именем `multiply_default`.
# Функция должна принимать два числовых аргумента:
#   `x` (обязательный)
#   `y` (необязательный, по умолчанию 2)
# Функция должна ВОЗВРАЩАТЬ (return) произведение x и y.
# Вызовите функцию два раза и выведите результаты:
# 1) С одним аргументом: x=10 (y должно быть 2 по умолчанию).
# 2) С двумя аргументами: x=5, y=4.
def multiply_default(x, y=2):
    return x * y
print(multiply_default(10))
print(multiply_default(5,4))
